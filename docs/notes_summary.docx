   {/* Recent Notes Summary */}
        {notes.length > 0 && (
          <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-100">
            <h3 className="text-lg font-semibold text-gray-900 mb-4">Recent Notes Across All Books</h3>
            <div className="space-y-3">
              {notes.slice(0, 5).map(note => (
                <div key={note.id} className="p-3 bg-gray-50 rounded-lg">
                  <div className="flex items-start justify-between">
                    <div className="flex-1">
                      <div className="flex items-center space-x-2 mb-1">
                        <NoteTypeIcon type={note.type} />
                        <span className="text-sm font-medium text-gray-700">
                          {note.books?.title || 'Unknown Book'}
                        </span>
                        {note.page_number && (
                          <span className="text-xs text-gray-500">• Page {note.page_number}</span>
                        )}
                      </div>
                      <p className="text-sm text-gray-900 line-clamp-2">{note.note}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        )}
      </div>
    );
  } 
