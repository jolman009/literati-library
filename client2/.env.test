# Test Environment Configuration for Client
# This file is used during CI/CD test runs (E2E tests)

# API Configuration - Test endpoints
VITE_API_BASE_URL=http://localhost:5000
VITE_AI_SERVICE_URL=http://localhost:8000

# Service Worker - Disabled in tests
VITE_ENABLE_SERVICE_WORKER=false

# Debug Mode
VITE_DEBUG_MODE=false

# Supabase Configuration (test/mock values)
VITE_SUPABASE_URL=https://test.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key

# Environment identification
VITE_APP_ENV=test

# Sentry (disabled in tests)
VITE_SENTRY_DSN=
VITE_APP_VERSION=test

# Authentication
VITE_DEV_HEADER_AUTH=false

# Google Integration (test values)
VITE_GOOGLE_CLIENT_ID=test-client-id
VITE_GOOGLE_API_KEY=test-api-key
