<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions for TWA -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Optional permissions for enhanced functionality -->
    <!-- Android 13+: Use system Photo Picker / SAF for file selection (no broad media/storage permissions) -->
    <!-- <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> intentionally removed -->

    <!-- Notification permissions for reading reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- No MANAGE_EXTERNAL_STORAGE: rely on system pickers and scoped access -->

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.ShelfQuest.Splash"
        android:supportsRtl="true"
        android:targetSandboxVersion="1"
        tools:targetApi="34">

        <!-- Splash Screen Activity -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ShelfQuest.Splash">

            <!-- Main App Intent Filter -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Link Intent Filters - Production Domain -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="shelfquest.org" />
            </intent-filter>

            <!-- Alternative domain support - www subdomain -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="www.shelfquest.org" />
            </intent-filter>

            <!-- File sharing intent filters -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/epub+zip" />
            </intent-filter>

            <!-- TWA Configuration -->
            <meta-data android:name="android.support.customtabs.trusted.DEFAULT_URL"
                       android:value="https://shelfquest.org" />

            <!-- TWA Display Mode -->
            <meta-data android:name="android.support.customtabs.trusted.DISPLAY_MODE"
                       android:value="standalone" />

            <!-- Status Bar Color -->
            <meta-data android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                       android:resource="@color/md_theme_primary" />

            <!-- Navigation Bar Color -->
            <meta-data android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                       android:resource="@color/md_theme_surface" />

            <!-- Share Target for file uploads -->
            <meta-data android:name="android.support.customtabs.trusted.SHARE_TARGET"
                       android:resource="@xml/share_target" />

            <!-- File Provider Authority -->
            <meta-data android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
                       android:value="org.shelfquest.app.fileprovider" />

            <!-- Splash Screen Duration -->
            <meta-data android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                       android:value="300" />

            <!-- Splash Screen Background Color -->
            <meta-data android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                       android:resource="@color/md_theme_primary" />
        </activity>

        <!-- TWA Service for Digital Asset Links verification -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.TwaLauncherActivity"
            android:exported="false" />

        <!-- File Provider for sharing functionality -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="org.shelfquest.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Notification Service -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.NotificationService"
            android:exported="false" />

        <!-- Chrome Custom Tabs Service -->
        <service android:name="androidx.browser.customtabs.CustomTabsService"
                 android:exported="true">
            <intent-filter>
                <action android:name="android.support.customtabs.action.CustomTabsService" />
            </intent-filter>
        </service>

    </application>

    <!-- Queries for package visibility (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
        <package android:name="com.android.chrome" />
        <package android:name="org.chromium.chrome" />
        <package android:name="com.chrome.beta" />
        <package android:name="com.chrome.dev" />
        <package android:name="com.google.android.apps.chrome" />
    </queries>

</manifest>
